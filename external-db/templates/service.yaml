apiVersion: v1
kind: Service
metadata:
  name: {{ include "external-db.fullname" . }}
  labels:
    {{- include "external-db.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "external-db.selectorLabels" . | nindent 4 }}
